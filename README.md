## Environment Setup

- node: version 20 (`nvm use 20`)
- Python: FastAPI

## Start Commands

- Frontend: in `/frontend` run `npm i react-plotly.js plotly.js` and `npm install`, then run `npm run dev`
- Backend: in `/backend` run `uvicorn app:app --reload --port 8000`
